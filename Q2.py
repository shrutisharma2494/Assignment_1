# Write a Python program that accepts a word from the user and reverse it.

data =str(input("Enter a word: "))
backwards = data[::-1]
print("Word is: ",data)
print("Reversed word is: ",backwards)